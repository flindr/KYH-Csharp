@using CoolPage.Models;
@model User;

<div>
    @if(Model != null)
    {
        <h1>Tjaaaaa @Model.Name</h1>
        @if(Model.Coolness == double.PositiveInfinity)
        {
            <h1>😎😎😎😎😎😎</h1>
        }
    }
    
    <p>Detta är en paragraf</p>
</div>